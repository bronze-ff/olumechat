interface:
  display_name: "Olumechat"
  short_description: "Repo-specific patterns and workflows for olumechat"
  default_prompt: "Use the olumechat repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true